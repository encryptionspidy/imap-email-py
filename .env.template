# Multi-Provider Email CLI Environment Template
# Configure your email credentials and settings

# Set your email address and app password or regular password
EMAIL_USER=your_email@example.com
EMAIL_PASSWORD=your_app_password_here

# Specify your email provider (e.g., gmail, outlook, yahoo, tuta, protonmail, icloud, fastmail, custom)
EMAIL_PROVIDER=gmail

# If using 'custom', provide additional IMAP settings
# IMAP_HOST=custom_imap_host
# IMAP_PORT=993
# IMAP_USE_SSL=true  # true or false
# IMAP_FOLDER=INBOX

# Optional: Customize database paths, FAISS index paths, etc.
# DB_PATH=/path/to/emails.db
# FAISS_INDEX_PATH=/path/to/faiss_index.bin
# FAISS_MAPPING_PATH=/path/to/faiss_mapping.json
